create table product (
                         id bigint not null,
                         description varchar(255),
                         name varchar(255),
                         price double not null,
                         primary key (id)
);